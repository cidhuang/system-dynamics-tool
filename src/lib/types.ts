/**
 * The poisition of a point
 * @property x x of point
 * @prop y y of point
 * @interface
 */
export interface Point {
  x: number;
  y: number;
}

export interface Variable {
  name: string;
  text: string;
  x: number;
  y: number;
}
